import { Loading } from "@krtech-digital/krtech-ui";

const Test = () => {
  return (
    <div
      style={{
        display: `flex`,
        justifyContent: `center`,
        alignItems: `center`,
        width: `100vw`,
        height: `100vh`,
      }}
    >
      <Loading />
    </div>
  );
};

export default Test;
